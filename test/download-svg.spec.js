const { downloadSvgFiles } = require('../scripts/download-svg')

describe.only('# download-svg', () => {
  before(() => {
    // downloadSvgFiles({})
  })
  it('t', () => {
    console.log('t')
  })
})
